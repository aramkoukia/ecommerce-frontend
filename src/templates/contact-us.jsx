import React from 'react';
import Layout from '../components/Layout';

const ContactUsTemplate = ({ data, classes }) => (
  <Layout>
    <h1>Contact Us</h1>
  </Layout>
);

export default ContactUsTemplate;
